 $object = json_decode($description,true);
             $name =  $object[0]['label'];
             $qta = $object[0]['quantity'];
             $image = $object[0]['image'];

*********************************************************
 count($json);